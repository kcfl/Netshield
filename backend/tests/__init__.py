"""Backend unit tests for NetShield.

These tests are intentionally lightweight and rely only on the Python stdlib
`unittest` module so they can run in the demo environment without extra
dependencies.
"""

